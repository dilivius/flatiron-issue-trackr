<li class="list-group-item">
  <span class="badge" data-repo="<%=repo.id%>" id="issue-count"><%=repo.issues.count%></span>
  <strong><%=RepositoryDecorator.new(repo).display_name %></strong>
    <%= render partial: "issues/issue", collection: repo.issues%>
</li>